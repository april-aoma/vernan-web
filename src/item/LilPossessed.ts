/**
 * LilPossessed familiar — deferred this pass.
 *
 * Java (`LilPossessed.java`): trails Vernan via spring follow (FOLLOW_K=95), fires 8-dir
 * snapped Possessed.Bullet on attack rising edge (or lemon shot), damage 0.5, cooldown 0.9s.
 * Assets exist: `lil possessed friend.png` + `.rig.json`, `lil possessed bullet.png`.
 *
 * Head item effect is live (`PossessedHead.ts`); familiar combat entity is stubbed until a
 * follow-up pass.
 */
export const LIL_POSSESSED_FAMILIAR_STUBBED = true;
